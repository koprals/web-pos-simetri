{
  "private": true,
  "type": "module",
  "scripts": {
      "dev": "vite",
      "build": "vite build",
      "heroku-postbuild": "npm install && npm run build"
  },
  "dependencies": {
      "axios": "^1.6.4"
  },
  "devDependencies": {
      "laravel-vite-plugin": "^1.0",
      "vite": "^5.0"
  }
}
